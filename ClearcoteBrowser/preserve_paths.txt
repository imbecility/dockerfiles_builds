# Пути ВНУТРИ собираемого образа, которые нужно забронировать явно
# через `--preserve-path`, независимо от того, что увидел динамический
# анализ SlimToolkit за время теста.
#
# Резервирование /usr/lib, /lib и шрифтовых директорий проверено и не
# повлияло на итоговый краш — значит, дело не в файлах. Не раздуваем
# образ ими без нужды.

/root/.cache/clearcote
/root/.clearcote
/app/.venv