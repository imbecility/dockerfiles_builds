# Дополнительные флаги для `docker run` на шаге верификации минифицированного
# образа (и, если понадобится, в проде). Один флаг (или пара флаг+значение)
# на строку, "#" — комментарий, пустые строки игнорируются.
#
# Для ClearcoteBrowser нужен --shm-size=1g — дефолтные 64MB /dev/shm слишком
# малы для движка персон/canvas-подмены и приводят к тихому закрытию браузера
# сразу после старта CDP.

--shm-size=1g