# Пути ВНУТРИ собираемого образа, которые нужно забронировать явно
# через `--preserve-path`, независимо от того, что увидел динамический
# анализ SlimToolkit за время теста.
#
# Формат: один путь на строку, "#" — комментарий, пустые строки игнорируются.
# Путь должен существовать именно в образе (проверяется на этапе сборки
# через `docker run --rm <образ> test -e <путь>` в ci.yml).

# /home/name/.cache/camoufox
/root/.cache/camoufox
/root/.camoufox

/usr/sbin/dnsmasq
/usr/sbin/gosu
/etc/dnsmasq.conf
/etc/dnsmasq.hosts
/app/start_dns.sh
/app/build_dns_blocklist.py
/app/dns_sinkhole
