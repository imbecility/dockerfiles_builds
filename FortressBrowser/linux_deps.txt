# Системные пакеты, нужные ТОЛЬКО для выполнения test.py на раннере
# (или локально), т.к. тест запускается через `slim build --host-exec`,
# то есть вне контейнера. В финальный образ эти пакеты НЕ ставятся —
# для этого используйте requirements.txt / Dockerfile.
#
# Формат: один пакет apt на строку, "#" — комментарий, пустые строки игнорируются.

ffmpeg
